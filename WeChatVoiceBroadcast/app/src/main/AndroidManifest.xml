<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.voicebroadcast"
    android:versionCode="1"
    android:versionName="1.0.0">

    <!-- Xposed 模块声明 -->
    <uses-sdk android:minSdkVersion="24" android:targetSdkVersion="35" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="微信语音播报"
        android:theme="@style/Theme.AppCompat">

        <!-- 主设置 Activity -->
        <activity
            android:name=".ui.SettingsActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.DayNight.DialogWhenLarge">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Xposed 入口 BroadcastReceiver -->
        <receiver
            android:name=".hook.ModuleReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="de.robv.android.xposed.IXposedHookLoadPackage" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
